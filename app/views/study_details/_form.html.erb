<%= form_with(model: study_detail) do |form| %>
  <% if study_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(study_detail.errors.count, "error") %> prohibited this study_detail from being saved:</h2>

      <ul>
        <% study_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :year, style: "display: block" %>
    <%= form.number_field :year %>
  </div>

  <div>
    <%= form.label :course_name, style: "display: block" %>
    <%= form.text_field :course_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
